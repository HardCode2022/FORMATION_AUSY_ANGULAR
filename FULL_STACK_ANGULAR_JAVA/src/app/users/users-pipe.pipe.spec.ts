import { UsersPipePipe } from './users-pipe.pipe';

describe('UsersPipePipe', () => {
  it('create an instance', () => {
    const pipe = new UsersPipePipe();
    expect(pipe).toBeTruthy();
  });
});
